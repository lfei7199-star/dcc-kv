# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
.venv/
.env

# pytest
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.cache
coverage.xml
*.cover

# mypy / flake8
.mypy_cache/
.dmypy.json
dmypy.json
.flake8

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb

# =============================================================================
# 项目特定
# =============================================================================

# 模型权重（绝对不能上传——大、可能违反许可）
models/
weights/
checkpoints/
*.bin
*.safetensors
*.pt
*.pth
*.pkl
*.ckpt

# 数据集
data/
datasets/
*.parquet
*.arrow
.cache/huggingface/

# 运行进程与临时试验产物（量大、不可重现）
runs/
logs/
wandb/
mlruns/
tensorboard_logs/
*.log

# Profiling trace（量大）
traces/
*.nsys-rep
*.nsys-sqlite
*.qdrep
*.qdstrm
*.prof

# 临时文件
*.tmp
*.bak
*.swp
*.orig
.cache/

# 一次性确定性补丁脚本（2026-09-21 决定）
# 体例：这类脚本承载的是「某次改动是怎么打的」，改动本身入库、脚本不入库
# （见 docs/commit_log.md）。此前它们散落在 scripts/ 下且未被跟踪，全靠
# 「记得别用 git add -A」这条人肉约定——现已移出仓库（归档见仓库外
# `_archive/spent_patches/ARCHIVE_NOTE.md`）。此条把约定从「靠人记」升级为
# 「结构上挡住」，因为 `git add -A` 吞临时文件的事在本仓库已犯过两次。
scripts/.patch_*.py

# 敏感信息
.env
.env.local
*.key
*.pem
secrets.yaml

# OS
Thumbs.db
.DS_Store

# 编译产物
*.o
*.a
*.so
*.dll
*.dylib

# LaTeX
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.synctex.gz
*.fls
*.fdb_latexmk

# 实验结果**入库**（2026-09-16 决定）：论文中的每个 CPU 数字都要能随仓库复现，
# 故 results/ 不再被忽略。此条必须排在 LaTeX 段的 `*.log` 之后，否则
# results/cpu/*_run.log 仍会被 *.log 挡掉。
!results/**

# 允许的例外（README 里的引用等）
!README.md
!docs/**/*.md
!docs/**/*.bib
!configs/*.yaml
