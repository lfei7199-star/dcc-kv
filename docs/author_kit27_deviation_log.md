# AuthorKit27 原件偏离台账

**原件**：`C:/Users/29914/Desktop/AuthorKit27 (1).pdf`（7 页；§1–4 的唯一权威来源）
**重建稿**：`paper/main.tex`（§1–4）+ `paper/sections/*.tex`（§5–8）
**建立**：2026-09-22
**修订**：2026-09-22（第二次）—— 补登 D2–D7。原版只登了 D1 并宣称「内容级偏离总数 = 1」，
经机器级 CJK 序列比对发现 §1–4 尚有 4 项内容级新增与 2 项零语义改动未登，**属台账自身规则的
违反**（规则见下第 1 条），故本次全部并入。

> **原件副本核验**（2026-09-22）：资料库个人空间存有同源副本 `AuthorKit27 (1)`。
> 本次将二者做归一化（去全部空白）后比对 —— **7033 字符全等**，且 PyMuPDF 逐页
> `get_images()` 核出**原件 7 页内嵌位图总数 = 0**。⇒ 本条台帐所依据的「原件无图无表」是实测事实。

## 这份文档做什么

逐条登记 `paper/` 相对 **AuthorKit27 原件**的**每一处有意偏离**。判据很硬：

> 凡是重建稿与原件不一致的地方，这里必须有对应条目；**没有条目的一致性缺口，一律视为缺陷，而不是改动。**

三条维护规则：

1. **原件是 PDF，不可改写。** 所谓「修改原件」在工程上只能是：**在重建稿里偏离，然后在这里记账**。
   因此本台账就是「对原件的修改」的完整可见面 —— 想核对改了什么，只看这里即可。
2. 每条必须给齐**五要素**：位置 / 原件原文 / 重建稿改后 / 理由与依据 / 关联提交。
3. **不编撰数据。** 若某处改动需要数字支撑，该数字必须能从 `results/` 落盘复现；
   做不到就不写进论文，只记在这里。

## 范围

| 章节 | 在原件内 | 说明 |
|---|---|---|
| §1 引言 | ✔ | 原件 §1 |
| §2 相关工作 | ✔ | 原件 §2（2.1–2.3） |
| §3 问题定义 | ✔ | 原件 §3（3.1–3.2） |
| §4 方法 | ✔ | 原件 §4（4.1 / 4.2 / 4.3.1–4.3.3 / 4.4） |
| §5 分析 | ✘ | 本文新增，不存在「偏离」 |
| §6 实验 | ✘ | 本文新增 |
| §7 讨论 | ✘ | 本文新增 |
| §8 结论 | ✘ | 本文新增 |

⇒ **本台账只覆盖 §1–4。** §5–8 的修订记在 `docs/commit_log.md`，不要混进来。

## 汇总

| # | 日期 | 位置 | 类型 | 一句话 | 关联提交 |
|---|---|---|---|---|---|
| **D1** | 2026-09-22 | §4.3.2 式(16) | **内容增强** | 补录 β 的箱约束 $\beta \in [-3,3]$（原件只有下界 $w \ge 0$） | 见 `commit_log.md` 第 36 条 |
| **D2** | 2026-09-13 | §4.1 图 1 | **插图新增** | `fig1_architecture.pdf` 总体架构图（原件无图） | `f0677f4` |
| **D3** | 2026-09-13 | §4.4 图 2 | **插图新增** | `fig2_async_pipeline.pdf` 异步流水图（原件无图） | `f0677f4` |
| **D4** | 2026-09-13 | §2.2 | **内容新增** | StreamingLLM 一句及其引用（原件无此句） | `f0677f4` |
| **D5** | 2026-09-13 | §4.4 归并算子 | **引文补全** | 补 `\cite{milakov2018online}`（原件该处无引用标记） | `907d356` |
| D6 | 重建初版 | §3.1 及全文 | 排版（零语义） | 原件的 $D$ → 宏 `\Ds`（`\mathcal{D}`） | 见 `f0677f4` |
| D7 | 2026-09-22 | §4.3.2 / Algorithm 1 | 机械（零语义） | 拆式补引导词「其中」×1；裸 `(n)` → `式~\eqref{}`×5 | `ad2f091` / 见第 33 条 |
| D0 | 2026-09-21 前 | 全文 | 格式（零文字偏离） | 式编号硬编码 → `\eqref`（16 处） | 见 `commit_log.md` 第 33 条 |

**分级统计（§1–4，共 7 条 + D0）**

| 级别 | 条数 | 条目 | 判据 |
|---|---|---|---|
| **内容级**（读者可见的正文内容发生变化） | **5** | D1 D2 D3 D4 D5 | 新增/改变了公式可行域、句子、图、引文 |
| 排版级（字形/宏，零语义） | 1 | D6 | 同一数学对象的记号写法 |
| 机械级（零语义，改造的连带产物） | 1 | D7 | 拆式引导词、引用风格统一 |
| 格式级（零文字偏离） | 1 | D0 | 只改引用机制，正文逐字未动 |

> ⇒ **当前 §1–4 的内容级偏离总数 = 5（D1–D5）。** 其余 3 条均**不改变论文语义**。
> 上一版台账写的「= 1」是**漏登**，不是口径差异 —— 依据是它自己的判据：
> 「没有条目的一致性缺口，一律视为缺陷，而不是改动。」

---

## D1 — §4.3.2 式(16)：补录 β 的箱约束 $\beta \in [-3,3]$

**裁定**：2026-09-22，西瓜先生裁定「修改箱约束 β∈[−3,3]，但是要在一个文档中汇总说明对原件的
修改具体内容」—— 本文件的由来即此。

**位置**：`paper/main.tex` 第 336–341 行（`\label{eq:beta-fit}`）。
对应原件 §4.3.2「注意力质量偏置拟合」（原件抽取文本第 268–312 行）。

### 原件原文（逐字，公式按抽取碎片还原）

> 仅保留 Key 子集会减小远程块对全局 Softmax 分母的贡献。对代表 Query $\widehat{q}_{r,a}$，定义完整块的
> Logit 与非归一化质量，以及紧凑 Key 的响应矩阵：……（式 15）
>
> 其中 $m^{s\to r} \in \mathbb{R}^M$，$G^{s\to r} \in \mathbb{R}^{M \times B_{s,r}}$。令
> $w_j = \exp(\beta_j) \ge 0$，则带偏置紧凑块的质量为 $\widehat{m} = Gw$。我们通过**非负岭回归**拟合
>
> $$w^* = \arg\min_{w \ge 0} \left\|G^{s \to r} w - m^{s \to r}\right\|_2^2 + \lambda_\beta \left\|w - \mathbf{1}\right\|_2^2,
> \qquad \beta_j^{s \to r} = \log\left(\max\{w_j^*, \varepsilon\}\right). \tag{16}$$
>
> $w_j$ 表示紧凑 Key 所承载的等效注意力质量，可聚合若干被删除 Key 的指数响应；正则项在拟合信息不足时
> 将其收缩至无偏置状态 $w = \mathbf{1}$。**非负约束保证 $w$ 可映射为 Logit 偏置。** 实现中应以逐行
> 最大值平移计算指数项，并在拟合目标中保留相应的行缩放，以避免数值溢出。

⇒ 原件式(16) 的可行域**只有下界 $w \ge 0$**（等价于 $\beta$ 只有下界、无上界），**没有箱约束**。

### 重建稿改后

```latex
\begin{equation}
w^* = \arg\min_{e^{-3} \le w \le e^{3}} \left\|G^{s \to r} w - m^{s \to r}\right\|_2^2
      + \lambda_\beta \left\|w - \mathbf{1}\right\|_2^2,
\label{eq:beta-fit}
\end{equation}
其中 $\beta_j^{s \to r} = \log(\max\{w_j^*, \eps\})$。
```

> **为什么 β 的定义在式外**：它原本与目标函数同处一个 `equation`。但 `acmart` 下在 `equation` 内嵌 `split`/`gathered`、且下标含 `\le`（这里是 `e^{-3} \le w \le e^{3}`）时，xelatex 会报 `! Argument of \split has an extra }.`（实测 split 形态 22 个错误、gathered 形态 1 个错误，单行形态 0 个错误；最小 article 文档复现不出）。**这是排版约束，不是内容偏离。**

并在式后补一句说明：箱约束等价于 $\beta_j^{s \to r} \in [-3,3]$，取自源论文附录 C.2 对 RMS 选键
分支的配置。

**改的是可行域的“上界”，不是下界。** $e^{-3} > 0$ 已经蕴含 $w \ge 0$，故原件「非负约束」的表述被
**包含**而非被替换 —— 这一点是刻意保持的，避免削弱原件论点的连续性。

### 为什么改

1. **实现本来就是这样跑的，属于「实现先于纸面」。**
   `src/dcc_kv_ref/calibration.py:215-216` 把求解投影到 $[\exp(-\beta_{\text{bound}}), \exp(\beta_{\text{bound}})]$
   的 $w$ 箱内（第 138 行做双侧 clamp）；`calibration.py:231` 再把 $\beta$ 二次 clamp 到 $[-3,3]$。
   默认 `DEFAULT_BETA_BOUND = 3.0`（`calibration.py:60`）。即**箱约束自始存在，只是式(16) 没写**。
2. **§6 早已披露它，§4 却未定义 ⇒ 存在正文内部缺口。**
   `paper/sections/06-experiment.tex:54-55` 写明「β 的校准参数在当前实现中取固定默认值：箱约束
   $[-3,3]$ 与 $\lambda_\beta = 3\times10^{-2}$」，表 `tab:knob` 的 caption（同文件 :631）也标了
   「箱约束 $[-3,3]$」。补录式(16) 是**让 §4 的定义与 §6 的实现声明一致**，消除「正文不写、实验却用」。
3. **依据可追溯，非本文发明。** $[-3,3]$ 取自源论文 Attention Matching 附录 C.2 对 RMS 选键分支的配置；
   `calibration.py:187` 与 `compact_kv.py:80` 的 docstring 均如此注明。
4. **实测无副作用，且它不是惰性项。**
   一次性诊断（`_onetime_scripts/checks/_diag_box_constraint.py`，产物 `_diag_out/box_constraint/`，
   box on/off × {$\lambda_\beta = 3\times10^{-2}$, $0$} 对照）：默认 $\lambda_\beta$ 下箱约束的
   **中位绑定率为 $0$**、单格最大 $|\Delta|$ 约 $0.038\%$；而 $\lambda_\beta = 0$ 时中位绑定率升到
   $6.25\%$ ⇒ **它在正则项缺席时确实承压**，属于第二道防线而非摆设。

### 影响面

- §4.3.2 的公式表述改变 ⇒ 本文 §1–4 出现**第一处内容级偏离**（即 D1）。
- **§6 无需改动**（本来就写了箱约束）。
- **数值结论不受影响**：实测影响量级 $0.038\%$，远小于 E9/E11 的格间散布。
- **算法伪代码无需改**：原件 §4.4 步骤 7「用 eq. (16) 拟合 $\beta^{s\to r}$」引用的是式(16) 本身，
  式(16) 内容变了它自动跟随。
- **式编号不变**：`\label{eq:beta-fit}` 未动，全文 `\eqref{eq:beta-fit}` 引用不受影响。

---

## D2 — §4.1 图 1：新增总体架构图 `fig1_architecture.pdf`

**位置**：`paper/main.tex` L259–267（`\label{fig:arch}`），位于 §4.1「总体框架」末尾。
对应原件 §4.1 —— 原件该处**只有正文，无图、无图号、无 caption**。

### 原件原文

> （**无**。）实测依据：原件 7 页内嵌位图总数 = 0（PyMuPDF 逐页 `get_images()`）；
> 全文 `图1` / `图2` / `Figure` / `表1` / `Table` 关键词 count 全为 0。

### 重建稿改后

```latex
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig1_architecture.pdf}
\caption{DCC-KV 的总体架构。目的设备仅发送少量代表 Query $\widehat{Q}_r$（虚线，经 Ring
交换）；每个源设备针对每条有向边 $(s,r)$ 独立构造紧凑 KV $C^{s\to r}$（实线）。由于构造以
目的端 Query 为条件，同一源块对不同目的端产生不同表示
（$C^{s\to r_1} \neq C^{s\to r_2}$）。}
\label{fig:arch}
\end{figure}
```

图的源是 `paper/figures/fig1_architecture.tex`（TikZ），`.pdf` 由构建生成且**不入库**
（`paper/.gitignore` 覆盖 `figures/fig*.pdf`）⇒ 图与源码永不脱钩。

### 为什么加

1. 原件无图，但 §4 需要让读者在进入细则前建立「目的端条件化 + 逐边独立构造」的整体印象；
   该架构的两个要点（$C^{s\to r_1}\neq C^{s\to r_2}$、代表 Query 单程广播）在原件正文中
   本就存在，图只是**同一信息的可视化**，不引入新主张。
2. 与本工作流 §5–8 的论证配套：§5 的界、§6 的 E9/E10 都在讨论「逐边条件化」的代价与收益。

### 影响面

- **正文文字零改动**（caption 是新写的，但图外正文逐字未变）。
- 新增图号「图 1」，全文浮动体编号从无到有。§5–8 引用 `fig:arch` 处随之成立。

**关联提交**：`f0677f4`（2026-09-13 17:32，「续写第 5-8 章、重建参考文献库并补矢量图」）。

---

## D3 — §4.4 图 2：新增异步流水图 `fig2_async_pipeline.pdf`

**位置**：`paper/main.tex` L394–401（`\label{fig:async}`），位于 §4.4 式(27) 之后。

### 原件原文

> （**无**）—— 同 D2 的实测依据。

### 重建稿改后

```latex
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig2_async_pipeline.pdf}
\caption{异步 All-to-Allv 流水。上：通信流与计算流相互独立，远程块到达后立即启动该块的
注意力计算，无需等待全部消息。下：与"等全部消息到达再统一计算"的同步实现对比；理想流水下
总耗时由两阶段中较慢者支配，而非二者之和。}
\label{fig:async}
\end{figure}
```

源文件：`paper/figures/fig2_async_pipeline.tex`。

### 为什么加

**这张图承载的是本文唯一的原创系统贡献**（变长紧凑 KV 的异步 All-to-Allv + 顺序无关归并）。
原件 §4.4 用文字与 Algorithm 1 描述该流水，但没有把「通信流 / 计算流重叠」这一核心图景画出来。
加它是为了让本文的原创点在图层面可见 —— 属**表达增强**，输入信息完全来自原件 §4.4 正文。

### 影响面

- 新增图号「图 2」。
- caption 中「与同步实现对比」的表述是**定性**的，不含任何本文未测得的性能数字
  （与 README「不主张通信性能结果」的边界一致）。

**关联提交**：`f0677f4`。

---

## D4 — §2.2：新增 StreamingLLM 一句及其引用

**位置**：`paper/main.tex` L165–166（§2.2 相关工作·KV 缓存压缩）。

### 原件原文

> （**无此句**。）实测：原件的**全文**中 `StreamingLLM` 子串出现次数 = **0**
> （`AK27.find('StreamingLLM') == -1`）。原件 §2.2 在列完 H2O / Scissorhands / SnapKV /
> PyramidKV 之后，直接进入下一段，没有 sink 现象这一支。

### 重建稿改后

> ……这些工作说明，少量 KV 条目往往主导注意力输出，但直接丢弃低分条目会同时改变块内语义和
> 块间归一化质量。**StreamingLLM \cite{xiao2024streamingllm} 则揭示了 attention sink 现象并
> 给出流式场景下的保留策略。**

### 为什么加

1. **与 §4.4 / §5 的论证闭环**。原件的归并算子保留「本地块 + 远程块」的双路结构，
   其中本地块固定保留的做法与 attention sink 的观察同源；不点出这一支，§2.2 的综述会
   缺掉「为什么不能一律丢弃低分条目」这一环。
2. **可追溯**：引文 `xiao2024streamingllm` 见 `paper/literature/`，非自拟。

### 影响面

- §2.2 多一句 + 一条引用；`refs.bib` 相应多一条（重建期补齐 17 条之一）。
- **它使 §1–4 出现一处「原件没有的论断」**，因此必须在本台账可见。

**关联提交**：`f0677f4`。

---

## D5 — §4.4 归并算子：补 `\cite{milakov2018online}`，并把「定义」改为「沿用」

**位置**：`paper/main.tex` L418（式(26)–(27) 的归并算子 $\oplus$ 之前）。

### 原件原文（逐字）

> 对两个状态 $S_a$ 与 $S_b$，**定义**归并算子 $\oplus$：

### 重建稿改后

```latex
对两个状态 $S_a$ 与 $S_b$，沿用在线的归并算子 $\oplus$ \cite{milakov2018online}：
```

即两处改动：**（i）**「定义」→「沿用在线的」（动词替换）；**（ii）** 末尾补引用标记。

### 为什么改

1. **这是学术归属问题，不改就是误署。** 式(26)–(27) 的 $m_{ab}=\max(m_a,m_b)$、
   $l_{ab}=e^{m_a-m_{ab}}l_a+e^{m_b-m_{ab}}l_b$、$o_{ab}=e^{m_a-m_{ab}}o_a+e^{m_b-m_{ab}}o_b$
   正是 Online Softmax（Milakov & Gimelshein, 2018）的归并式。原件写「定义」会被读成本文首创。
   本文的**原创贡献是把它用于变长紧凑 KV 消息的顺序无关归并**，不是归并式本身 ——
   改后既保住贡献主张，又交代了出处。
2. **与 USER.md 记录的原创边界一致**：本项目明确不主张压缩机制（Key 选择 / 质量偏置 /
   Value 回归）为原创，归并算子同理。
3. 该引文的实现在 `src/dcc_kv_ref/online_softmax.py`，docstring 已注明出处。

### 影响面

- 属于 **§1–4 内容级偏离**（动词替换 + 新增引用），含义方向是**收紧**而非扩张贡献。
- 式(26)(27) 本身、编号、邻域文字均未动（该处是 13 个非等片段中唯一一个 `replace`）。

**关联提交**：`907d356`（2026-09-13 17:44，「修复参考文献排版错误并补齐两条未引用文献」）。

---

## D6 — 全文符号：原件的 $D$ → 宏 `\Ds`（排版级，零语义）

**位置**：`paper/main.tex` L45 定义 `\newcommand{\Ds}{\mathcal{D}}`；使用处见 §3.1（L205–206）
`令 $\Ds = \{0,\ldots,N-1\}$`。

### 原件原文

> 令 $D = \{0,\ldots,N-1\}$。

### 重建稿改后

> 令 $\Ds = \{0,\ldots,N-1\}$ ——`\Ds` 展开为 `\mathcal{D}`（花体 D）。

### 为什么改

纯记号统一：`\Ds` 宏化后可避免与后文其他 $D$ 类符号冲突，也便于整体换字形。
**数学对象、取值范围、语义均未变。**

> **一处诚实说明**：原件的**字形**无法从 PDF 抽取文本中恢复（抽取丢失字体信息，只能得到字符 `D`），
> 因此本条无法断言「原件用的是正体还是花体」。登记为偏离是按**保守口径** —— 字形可能有别，
> 故不声称「逐字未变」。

**关联提交**：`f0677f4`。

---

## D7 — 机械性改动：拆式引导词 ×1、引用风格 ×5（零语义）

**性质**：**不改变论文含义**，是 D1 与 D0 两项改造的连带产物。登记它，是因为台账判据要求
「没有条目的一致性缺口一律视为缺陷」—— 这 6 处确实与原件字面不同，所以必须可见。

### （i）拆式引导词「其中」×1

- **位置**：`paper/main.tex` L341 `其中 $\beta_j^{s \to r} = \log(\max\{w_j^*, \eps\})$。`
- **原件**：$\beta_j$ 的定义与目标函数**同处式(16) 一个 `equation` 内**，故无需引导词。
- **改后**：β 定义被拆到式外，故补引导词「其中」。
- **依据**：拆式的原因是 xelatex 排版约束（详见 D1 的说明：`equation` 内嵌 `split`/`gathered`
  且下标含 `\le` 时编译报错）。**是排版驱动，不是文字驱动的改写。**

### （ii）引用风格 裸 `(n)` → `式~\eqref{}` ×5

- **位置**：`paper/main.tex` L454 / L455 / L456 / L462 / L464（Algorithm 1 的五条 `\State`）。
- **原件**：Algorithm 1 直接用裸编号引用，如「用 (10) 选取 $C_k^{s \to r}$」。
- **改后**：`\State 用式~\eqref{eq:ck} 选取 $C_k^{s \to r}$` 等，共 5 处。
- **依据**：D0 把全文硬编码编号改为标签引用时，统一加了「式~」前缀。**字面新增 5 个「式」字，
  指向的公式与原件逐号相同。**
- **另注**：原件全文使用裸 `(n)` 引用共 32 处，重建稿在 §1–4 内**没有再出现任何裸编号引用**
  ⇒ 这 5 处是本类差异的全部。

**关联提交**：`ad2f091`（D1 同批）/ 见 `commit_log.md` 第 33 条（D0）。

---

## D0 — 全文：式编号硬编码 → `\eqref`（格式类，零文字偏离）

**性质**：**不是内容偏离**，此处登记是为了让「§1–4 与原件的关系」有唯一入口。

**做了什么**：`paper/main.tex` 新增 27 个 `eq:` 标签，连同 §5 原有 13 个，全文 40 个编号公式全部有标签；
16 处写死的数字引用（Algorithm 1 五处、§5 十处、§6 一处、§7 一处）改为 `\eqref`，残留硬编码为 0。

**对原件的影响**：**零**。公式本身、文字本身**逐字未变**，只有「引用机制」从写死数字改为标签引用。
改动前后第 1–4 章编号均为 (1)–(27)，与原件**逐号一致**。

**为什么记在这里**：它决定了「重建稿是否忠实」这一前提。若不做这一步，任何后续插入公式都会造成
**静默脱钩**，而脱钩后的重建稿与原件的一致性将无法自动验证。

---

## 重建忠实性核验（台账的“对照组”）

台账只记偏离，容易被误读成「重建稿基本是自己写的」。为此固定几条已核对项：

- **公式编号**：第 1–4 章 (1)–(27) 与原件**逐号一致**（见 D0）。
- **式(16) 邻域**：本次补录前，`main.tex:325-344` 与原件 §4.3.2 的文字**逐字一致**
  （含「非负约束保证 $w$ 可映射为 Logit 偏置」「逐行最大值平移」等实现性表述）。
- **作者信息**：`main.tex` L53–58 仍为占位符，**不代填**（原样保留）。
- **正文 CJK 序列机器比对**（2026-09-22，`_audit_tmp/_chk_ak27_fidelity.py`）：
  原件全文 3634 字 vs `main.tex` §1–4 3793 字，**非等片段仅 13 个**，且已**全部**归入本台账：

  | 片段 | 内容 | 归入 |
  |---|---|---|
  | #1 #2 | 标题 / 作者 / 摘要 / 关键词骨架 | LaTeX 命令承载，**非正文偏离**（无需登记） |
  | #3 | §2.2 StreamingLLM 句 | **D4** |
  | #4 | 图 1 caption | **D2** |
  | #5 | 拆式引导词「其中」 | **D7(i)** |
  | #6 | 箱约束说明句 | **D1** |
  | #7 | 图 2 caption | **D3** |
  | #8 | 「定义」→「沿用在线的」 | **D5** |
  | #9–#13 | 「式」×5 | **D7(ii)** |

  ⇒ **不存在「未登记的一致性缺口」。** 这正是本条台账存在的意义：让「13 个差异 = 7 条登记 + 2 条骨架」
  这一等式随时可复算。

## 与 `writing_scope_and_metrics.md` 的关系

`docs/writing_scope_and_metrics.md` §0.1 也列过「重建时相对原件的新增」四项 —— 那是本条台账的
**前身**，口径偏松（把 fig1/fig2 合成一项、把 `\Ds` 与内容级并列、未含 D7）。

**两者分工现已划定，勿混用：**

| 文件 | 职责 |
|---|---|
| **本台账** | **§1–4 相对原件的偏离**（唯一权威）。想知道「改了什么」，只看这里 |
| `writing_scope_and_metrics.md` | 原件条款 → §5–8 应报指标的**口径映射**清单，不承担偏离登记 |

维护提示：新增偏离时在「汇总」表上方插入 D8、D9 …… 并按 D1 的五要素结构书写；
**不要把 §5–8 的改动登记进来**。

