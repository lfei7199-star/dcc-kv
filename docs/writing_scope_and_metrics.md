# 撰写范围与指标口径

> **本文件的用途**：以桌面 `AuthorKit27 (1).pdf`（第 1–4 章的**唯一权威原件**）为标尺，
> 逐条抽出其中的「可测量定义」，映射到第 5–8 章应当报告的指标、每个指标的精确口径、
> 当前证据与缺口。它是撰写与审稿的对照清单，不是论文正文。
>
> 生成时间：2026-09-16；第八轮（E12 / E13 / E3 留出重跑）后更新，
> 关闭项见 §4 后附注与 §6.4，对应提交见 `docs/commit_log.md` 第 27 条。

---

## 0. 标尺与现状

**原件范围**：标题、摘要、1 引言、2 相关工作、3 问题定义、4 方法 + Algorithm 1，共 7 页。
**原件不含第 5–8 章** —— 本文件的「方向」即针对第 5–8 章。

| 项 | 状态 |
|---|---|
| 第 1–4 章 | 仓库内为**重建版**（无原始 `.tex`）；公式编号已与原件**逐号对齐**为 (1)–(27) |
| 第 5–8 章 | 仓库内为本工作流续写 |
| 原件中的图 | **无**。现稿的 fig1/fig2（架构、异步流水）是重建时新增 |
| 原件中的引用 | 全部为 `[?]`（未接 bib）。现稿的 17 条 `refs.bib` 为重建时补齐 |

### 0.1 重建时相对原件的新增（须知情，非原件内容）

1. `\cite{milakov2018online}`（在线 Softmax 归并算子的出处）—— 原件该处无引用标记。
2. §2.2 中 StreamingLLM 一句及其引用 —— 原件无此句。
3. fig1（总体架构）、fig2（异步流水）两张图 —— 原件无图。
4. 符号 `\Ds`（原件写作 `D`）—— 仅排版差异。

除上述四项外，第 1–4 章的文字与公式与原件逐字对应。

---

## 1. 建模条款 → 应报指标

「条款」按原件公式编号引用（该编号现已与现稿一致）。

| 原件条款 | 原件中的定义 | 应报指标 | 口径要点 | 现稿位置 | 现状 |
|---|---|---|---|---|---|
| **(4)(5)** | 块级质量 $M_s(q)=\sum_j e^{\ell}$、条件输出 $O_s(q)$ | $\epsilon_{\text{mass}}$ 与 $\epsilon_{\text{out}}$ **必须分开报** | **理论量是跨块求和的绝对量**；实验量是单块相对量 —— 见 §2 | §5 式(35)(36) | 合成数据已有；**口径对照表已进 §6.2**（`tab:metrics`，**M1 已关闭**） |
| **(6)(20)** | 全局注意力 = 质量加权混合 $\frac{\sum_s M_sO_s}{\sum_s M_s}$ | 输出误差 $\|\widehat{\mathrm{Attn}}-\mathrm{Attn}\|_2$ 及其**界的紧致度** | 以全局 $S(q)$ 归一 | §5 式(37) | **M3 已关闭**：E13 逐 Query 检验，$5760$ Query 零反例、$\mathrm{RHS}/\mathrm{LHS}$ 中位 $2.10$ |
| **(7)(8)** | $C^{s\to r}=(C_k,\beta,C_v)$，$B_{s,r}\ll L_s$ | 压缩率 $B/L_s$；且**必须同报 $M$ 与 $B_{\text{eff}}/B$** | $B_{\text{eff}}$ = $V$ 回归设计矩阵的数值秩估计 | §6 E2/E9/E10/E13 | 已有（E9 375 格、E10 180 格）；E13 另确立 $\beta$ 的第三个作用 |
| **(9)** | $C^{s\to r_1}\neq C^{s\to r_2}$ | KL、JS、选中索引 Jaccard **三项独立证据** | H1 判据为 KL **严格大于** $0.5$，且须与噪声底线比较 | §6 E3 | **M9 已关闭**：已按留出协议重跑（$18/20$、汇总 $-0.167$）；旧样本内落盘留作对照 |
| **(10)(11)(12)** | Rademacher 投影 + 最远点采样取 $M$ 个真实 Query | 摘要通信量 $(N-1)Md_h$；选择开销 $O(L_rd_hd_p+ML_rd_p)$ | $d_p$ 的取值影响 $\epsilon_{\text{repr}}$ | §5 式(28)(32) | **M2 已关闭**：E12 扫 $d_p\in\{4,\dots,64\}$，畸变单调降但下游饱和于 $d_p\ge8$ |
| **(13)(14)** | 跨 Query RMS 响应 Top-$B$ | 实际保留质量 vs 名义 $B/L$ | 名义压缩比系统性低估保留率 | §6 E2 | 已有（**FP64 重跑**：$B/L=0.031$ 时保留 $0.275$，$8.8\times$） |
| **(15)(16)** | 非负岭回归拟合 $w$，$\beta=\log w$ | clamp 率、$B_{\text{eff}}/B$、$V$ 残差、留出输出/归并 | 箱约束 $[-3,3]$ 取自**源论文 Appendix C.2**、非自拟；$\lambda_\beta=3\times10^{-2}$ 由 E11 定位 | §6 E10/E11 | 已有 180 格 ×2 |
| **(17)(18)(19)** | 多输出岭回归得 $C_v$ | $V$ 残差；$\mathrm{rank}(X)\le M$ 的欠定后果 | 有效秩上限是 $M$，不是 $B$ | §6 E9/E10 | 已有 |
| **(21)** | 接收 $\parallel$ 计算 $\parallel$ 发送 | $T_{\text{build}}$、$T_{\text{comm}}$、$T_{\text{comp}}$、$T_{\text{total}}$ **四项分开报** | 四项**不可合并**；计时窗口内只做 `cuda.synchronize`，不得含 `dist.barrier` | §6 E5/A5 | 框架已拆项，**数值全缺** |
| **(22)** | $\beta$ 直加在 logit 上 | $\beta$ 的系数口径 | **系数为 1**，拟合侧与推理侧一致 | §6 E2b | 判定完成 |
| **(23)–(27)** | 在线 Softmax 状态与归并算子 $\oplus$ | 乱序 / 树形归并相对顺序归并的最大相对误差 | 须报精度、块数 $K$、树形深度 | §6 E0 | FP32 $\sim10^{-7}$、FP64 $\sim10^{-16}$；**FP16/BF16 缺**（E8） |
| **Algorithm 1** | 单层单头流程 | 端到端数值等价性 | — | §6 E4 | 已有（gloo 两进程） |

---

## 2. 误差度量：论文术语 ↔ 代码函数 ↔ §5 定义

**这是当前最实质的文字缺口。** 论文中出现了六个误差术语，没有任何一处声明它们与 §5
的 $\epsilon_{\text{mass}}$、$\epsilon_{\text{out}}$ 的对应关系；代码中相应存在**四套并存**的
度量实现。下表是本文件的核心。

| 论文术语 | 代码函数（`experiments/common/synthetic.py`） | 口径 | 与 §5 理论量的关系 |
|---|---|---|---|
| $\epsilon_{\text{mass}}$（式 35） | — | 跨源块 $\sum_{s\in\mathcal{E}_r}$、绝对 | **理论量** |
| 「绝对质量误差」 | `absolute_mass_error` | 公共偏移 $c=\max(\ell_{\text{full}})$，单块 | 理论量的**单块版**（未跨块求和、未加权） |
| 「单块误差」 | `mass_error`（**旧口径，已废弃**） | 两侧各减自身最大值 ⇒ 对 $\beta$ 的**常数分量免疫** | **不得再用于 $\beta$ 相关结论** |
| $\epsilon_{\text{out}}$（式 36） | — | 跨源块 $\sum_s M_s(q)\|\cdot\|_2$、绝对 | **理论量** |
| 「留出输出」/「相对输出误差」 | `relative_output_error` | 单块，除以 $\|y_{\text{dense}}\|_2$ | 理论量的**单块归一化版** |
| 「留出归并」/「归并误差」 | `mixture_relative_error` | 源块 + 固定上下文块拼接后再与 Dense 比 | **最接近式(37) 的界所约束的量** |

### 2.1 由此得到的三条硬结论

1. **论文中所有实验数字都是「理论量的单块版」。** 而 §5 的界是跨块版。
   二者之间没有已测量的桥 ⇒ **式(37) 的界从未被数值检验过紧致程度**（缺口 M3）。
   正文现在把「误差不随块数发散」当作已论证的性质，但它依赖的是**未经检验**的界。
2. **报任何误差数字时必须同时声明三件事**：是单块还是归并侧、是否归一化、用哪套偏移。
   仅写「误差 0.42」在信息上是不完整的 —— 换一套度量可以得到完全不同的数。
3. **`mass_error`（旧口径）与 `absolute_mass_error`（新口径）的结果不可互比**；
   E2 的早期落盘用前者、E2b 用后者，二者在正文中并列出现时须知会读者。

---

## 3. 第 5–8 章的撰写方向

| 章 | 现状 | 方向 | 待补 |
|---|---|---|---|
| **5 复杂度与误差分析** | 完备，且已自律（图注「解析结果，非实测」、末尾自承未测量） | 保持三段结构：通信量 → 计算量 → 误差分解与界 | 误差界与实测的对接（**M3 已完成**）；代表 Query 开销的实测（**M2 已完成**） |
| **6 实验方案** | CPU 部分完备；GPU 部分为协议 | 保持「已执行 / 待执行」二分，任何未执行项不得出现具体数值 | 度量定义表（**M1 已完成**）；E0 的「置换数 × 树形」曲线；E3 的留出重跑（**M9 已完成**） |
| **7 讨论** | 本轮修正 3 处过时表述 | §7.3 已自承「压缩与异步存在张力」，该交互**未被任何单一实验捕捉** | 需在 E5/E6 的轴里增加「预算 × 同步/异步」二维格（M4） |
| **8 结论** | 完备 | 保持 | — |

---

## 4. 缺口清单与判定标准

每条给出「如何判定它已被解决」，避免缺口长期悬置。

| # | 缺口 | 判定标准 | 可做环境 |
|---|---|---|---|
| **M1** | 六个误差术语无口径声明，四套度量并存 | §6.2 增加「度量定义」段，逐术语写明：代码函数名、是否跨块、是否归一化、偏移方式 | CPU |
| **M2** | 代表 Query 选择全为解析，$d_p$ 与投影误差的关系从未测量 | 扫 $d_p\in\{8,16,32,64\}$，报留出 Query 上的覆盖率与 $\epsilon_{\text{mass}}/\epsilon_{\text{out}}$ 联合变化 | CPU |
| **M3** | 式(37) 的误差界从未被数值检验紧致度 | 在 E2/E2b 落盘数据上，逐 $B$ 计算「界右侧」与「实测左端」之比，报比值分布 | CPU |
| **M4** | 「压缩 × 异步」的交互无实验对应 | **本轮已补设计**：E5 新增 A4（预算 × 同步模式的二维格 + 两条可证伪预测 + 判定方式），见 §6.3。设计与 A2/A5 的分工写在论文 §6.3「A4」 | —（设计已定，执行待 GPU） |
| **M5** | `mass_error` 旧口径仍被 `e2_fidelity_curve.py:103`、`e5a_mechanism_ablation.py:110` 调用，其 docstring 仍在为已被推翻的做法辩护 | 给函数加弃用标注；确认 E2/E5a 落盘结果用的是哪个口径，并在正文注明 | CPU |
| **M9** | E3 的 H2 对照用 `dest_queries` 同一批 Query 既构造又评估（样本内） | 按留出 Query 重跑 E3 并同步正文数字；探针 `experiments/cpu/probe_e3_heldout.py` 已量化偏差（$\Delta$ 由 $-0.2454$ 缩至 $-0.1683$，方向 $20/20\to18/20$） | CPU |
| **M6** | H2 判据在论文中完全缺席 | **本轮已补**：§6.2「假设与判据」段，含 H1–H5 阈值与两条限定 | — |
| **M7** | 公式编号与原件错位，导致 §5/§6/§7/Algorithm 共 16 处引用静默指错 | **本轮已修**：编号对齐为 (1)–(27)，32 处引用改走 `\eqref`，残留硬编码为 0 | — |
| **M8** | §6 小结「20 组中 17 组显著、3 组不显著」与 E3 实测（20/20、18/20、18/20）矛盾 | **本轮已修** | — |

> **第八轮关闭情况**：上表 **M1**（§6.2 新增 `tab:metrics` 度量口径对照表，六个术语 ↔ 实现 ↔ 跨块/归一化逐项写明）、
> **M2**（E12：$d_p$ 扫描）、**M3**（E13：式 37 的逐 Query 紧致度与前置条件违反率）、
> **M5**（`mass_error` 加弃用标注，E2/E5a 调用方改 `absolute_mass_error`，落盘列名同步为
> `eps_mass_abscommon_*`）、**M9**（E3 按留出协议重跑并同步正文）——**五项全部关闭**。
> M4 / M6 / M7 / M8 已于更早轮次关闭。

---

## 5. 口径硬约束（不得违反）

1. **评估 Query 不得参与选键、$\beta$ 拟合或 $V$ 回归。** 否则「增大 $M$」自动获胜，
   结论退化为同义反复。E9 的 oracle 是**作弊上界**，只用于界定 $B$ 的表示能力，
   绝不可作为可实现精度报告。
2. **报「预算–精度」曲线时必须同报 $M$。** 否则读者无法区分「预算不够」与「估计不足」。
   A2 只能称**参考曲线**，不得称帕累托前沿（两轴当前不同源）。
3. **四项计时不可合并。** $T_{\text{build}}$ / $T_{\text{comm}}$ / $T_{\text{comp}}$ / $T_{\text{total}}$
   分开报；窗口内只做 `cuda.synchronize`，不得含 `dist.barrier`。
4. **阈值一律走单一事实源**（`experiments/common/hypotheses.py`），不得在脚本里写魔法数字。
5. **压缩机制（Key 选择 / $\beta$ / $V$ 回归）源自 Attention Matching，不得主张为原创。**
6. **不得主张**任务质量、通信性能、多卡可扩展性、与基线的对比优势 —— 这四项均无证据。
7. **拿不准的前提不得作为结论写入**（§6 小结末句）。

---

## 6. 裁决与落实

### 6.1 前两轮已修

- 公式编号与原件逐号对齐（M7）；32 处引用 `\eqref` 化。
- §6 小结 E3 计数改为与落盘产物一致（M8）。
- §7 三处：`10^{-2}` 量级的过度声称改为 E4 实测值；「ε 只有单点测量」与
  「M 只能由实验（A2 与 E2）回答」两处过时表述更新；删除对不存在「首页脚注」的引用。
- §6 随机性段与 `docs/reproducibility.md` §7 对齐（原文「备选 123/1024/7、每点 10 次」
  与代码实况不符）。
- §6.2 增补「假设与判据」段（M6）。

### 6.2 第四轮：用户裁决与落实（2026-09-16）

| # | 事项 | 裁决 | 落实 |
|---|---|---|---|
| **D1** | A4 消融的定义 | 原件第 1–4 章**通篇无「消融」字样、无 A1–A5 编号**（逐字检索确认），故按设计取「压缩 × 异步交互」 | §6 E5 新增 A4 段；状态总览表补编号；前置缺口清单把 A4 列入 (i)(ii) 阻断；§7.3 结尾指向 A4。**M4 随之关闭** |
| **D2** | H2「质量相近」的判定主体与容差 | 要求给出**独立讨论环节** | §7 新增小节「「质量相近」的判定」（标签 `sec:quality-comparable`，含式 `eq:delta-window`）；§6.2 改为指向它；定义落地到 `hypotheses.py`（`quality_comparable_non_inferior` + 两个常量）、`reproducibility.md` §6、`release_checklist.md` §4；`tests/test_hypotheses.py` 补 7 个锚点。**H2 仍为 `no-judge`**（见 §6.4 的 O1） |
| **D3** | `results/` 是否解除 gitignore | **提交至 GitHub** | `.gitignore` 删 `results/` 规则、末尾加 `!results/**`（必须排在 LaTeX 段 `*.log` 之后）；51 个文件入库；`FILE_MAP` §9 与 `git_strategy` §8.1 同步 |
| **D4** | 第 1–4 章是否以重建版定稿 | **暂按重建版定稿** | `main.tex` 头部撤掉「应以原始文件替换」的注记，改为「重建版即定稿；原件为桌面 PDF」 |
| **D5** | 作者信息与仓库链接是否进正文 | **暂不考虑写入正文** | `main.tex` L53–58 保留占位符，并把 TODO 注释改为「**有意留白**」，以免日后被当成遗漏 |
| **D6** | 阈值是否标注「待验假设」 | **是** | §6.2 段落标题改为「假设与判据（下列五条阈值均为待验假设）」，并在段首以独立句写明「不是已确立的验收标准、不得作为结论引用」 |

### 6.3 新增：A4 的设计要点（供 E5 执行时对照）

- **两轴**：预算 `B/L_s` 至少 4 档 × {同步, 异步}。**每格**报四项计时
  （`T_build` / `T_comm` / `T_comp` / `T_total`）与 p50 延迟。
- **两条可证伪预测**：① `S_obs(B)` 随 `B` 上升，并在 `T_comm ≈ T_comp` 的档位接近
  `eq:speedup-bound` 给出的上界；② `S_obs(B)` 与该上界之差随 `B` 减小而扩大。
- **判定**：四档两两比 95% CI。**全部重叠 ⇒ 报告「未观测到显著交互」**，此时 A2/A5 的
  边缘结果才可独立引用；否则必须报二维格。**「无交互」是有效结论而非失败**。
- **为什么单靠 A2 与 A5 不够**：A2 固定同步模式、A5 固定预算，二者各自把另一条轴边缘化 ——
  只有在无交互时，边缘结果才等于联合结果。
- **「同步」臂的口径来自原件**：原件通篇**未出现「同步」一词**（逐字检索：`同步` 0 次、
  `异步` 7 次），它把对照情形写作「等待全部通信完成后统一计算」，即论文 §5 的纯串行式。
  A4 / A5 的「同步」一律指该实现，不得理解成在设备间额外插一次屏障。
- **代码侧状态**：A4 **有设计、无代码**。`experiments/gpu/e5_gpu_ablation.py` 的消融注册表
  仍是 A1/A2/A3/A5，脚本 docstring 已把 A4 显式登记为「设计已定、代码待实现」；
  实现时机与 A3 相同（等 `CompactKV → attention kernel` 通路可用）。

### 6.4 仍开放（本文件不代为决定）

| # | 事项 | 为什么不能现在关闭 |
|---|---|---|
| **O1** | H2 的两个参数（非劣边界 `δ`、噪声底线） | 两者都依赖 E6 的重复 run；在拿到数据前 `δ` 只能给出**区间**（式 `eq:delta-window`），不能给点值。**这是 H2 判据未接的唯一剩余原因** |
| ~~**O2**~~ | ~~H2 跨上下文长度的聚合方式~~ | **第八轮已接线**：`hypotheses.py::h2_pass_across_lengths` 已接入 `e6_main_table.py` 的 `payload["h2"]`，两个前提参数以**无默认值**的命令行选项暴露（有默认值等于假称前提永远成立）；`tests/test_hypotheses.py` 补 10 个锚点 |
| ~~**O3**~~ | ~~缺口 M1 / M2 / M3 / M5 / M9~~ | **第八轮已全部关闭**，见 §4 后附注：M1 = 口径对照表进 §6.2；M2 = E12；M3 = E13；M5 = 弃用标注 + 调用方与列名同步；M9 = E3 留出重跑 |
| **O4** | `results/` 入库后的体积策略 | 本轮入库 8.13 MB（CPU 全部 + 一个 GPU 冒烟）。GPU 单次 run 规划为 1–100 MB，全量入库会迅速膨胀，届时应改按目录或按上限选择（已写入 `git_strategy.md` §8.1） |
